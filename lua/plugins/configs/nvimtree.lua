require'nvim-tree'.setup {
    open_on_setup = true,
    open_on_setup_file = false,
    open_on_tab = false,
    update_cwd = true,
    auto_reload_on_write = true,
    git = {
        enable = false
    }
}
